import { SSPManager } from '@/components/revenue/ssp-manager';

export default function SSPPage() {
  return <SSPManager />;
}

export const metadata = {
  title: 'SSP Management | GLAPI',
  description: 'Standalone Selling Price evidence and analysis',
};