import { Libraries } from '@/components/Libraries'

export const metadata = {
  title: 'Protocol SDKs',
  description:
    'Kurrent offers you the ability to fine tune your system settings.',
}

export const sections = [
  { title: 'Official libraries', id: 'official-libraries' },
]

# System Configuration

The platform allows for both system wide and project specific settings. The project takes priority meaning that you can override the system settings at the project level. {{ className: 'lead' }}

<Libraries />
